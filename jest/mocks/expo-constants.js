/**
 * Stub Jest pour expo-constants (module natif indisponible en environnement node).
 * Les tests qui ont besoin de valeurs spécifiques peuvent mocker src/config/env.
 */
module.exports = {
  expoConfig: { extra: {} },
};
